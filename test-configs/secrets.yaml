# Test secrets file - DO NOT USE REAL CREDENTIALS
# This is for configuration testing only

# WiFi credentials
wifi_ssid: "test-network"
wifi_password: "test-password"

# ESPHome credentials
ota_password: "test-ota-password"
